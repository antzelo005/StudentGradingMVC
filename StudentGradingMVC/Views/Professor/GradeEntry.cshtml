@model StudentGradingMVC.Models.ViewModels.ProfessorGradeEntrySelectCourseVM
@{
    ViewData["Title"] = "Grade Entry";
}

<h2 class="mb-3">Grade Entry</h2>

@if (Model.Courses == null || Model.Courses.Count == 0)
{
    <div class="alert alert-warning">No courses are currently available.</div>
}
else
{
    <div class="list-group">
        @foreach (var c in Model.Courses)
        {
            <a class="list-group-item list-group-item-action"
               asp-controller="Professor"
               asp-action="GradeEntryForm"
               asp-route-courseId="@c.CourseId">
                <div class="d-flex justify-content-between">
                    <strong>@c.Title</strong>
                    <span class="text-muted">Semester: @c.Semester</span>
                </div>
            </a>
        }
    </div>
}
