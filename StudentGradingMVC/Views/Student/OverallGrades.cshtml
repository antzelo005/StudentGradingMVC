@model decimal?
@{
    ViewData["Title"] = "Overall Grade";
}

<h2>Overall Grade</h2>

@if (Model == null)
{
    <div class="alert alert-info">No grades have been recorded yet.</div>
}
else
{
    <div class="alert alert-success">Average: <strong>@Model</strong></div>
}
